var ChatView = Backbone.View.extend({
    className: "chat",
    tagName: "div",

    initiialize: function(chat) {
        this.chat = chat || null;
    }

    render: function() {
        
    }
});
